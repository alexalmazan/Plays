-
 
   hosts: all
   tasks:

     - name: Gather facts on virtual machines
       local_action:
         module: vmware_vmkernel_ip_config
         hostname: icisr501.echristus.net"
         username: "domain\user"
         password: "$password"
         validate_certs: False
